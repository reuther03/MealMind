namespace MealMind.Shared.Abstractions.Services;

public interface IShippingService
{
    // Task<ShipmentDto> CreateShipmentAsync(CreateShipmentCommand cmd);
    // Task<IEnumerable<RateDto>> GetRatesAsync(Guid shipmentId);
    // Task<LabelDto> PurchaseLabelAsync(PurchaseLabelCommand cmd);
    // Task<TrackingInfoDto> GetTrackingAsync(Guid shipmentId);
}